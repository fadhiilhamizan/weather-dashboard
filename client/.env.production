# Frontend and backend are served from the SAME Vercel domain, so the API base
# is left empty on purpose: the client calls relative "/api/..." URLs, which
# Vercel routes to the serverless function in /api. No cross-origin, no CORS.
VITE_API_BASE_URL=
